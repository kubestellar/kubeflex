builds:
- id: "kflex"
  main: ./cmd/kflex
  binary: bin/kflex
  ldflags:
  - "{{ .Env.LDFLAGS }}"
  goos:
  - linux
  - darwin
  goarch:
  - amd64
  - arm64
  ignore:
  - goos: darwin
    goarch: ppc64le
  env:
  - CGO_ENABLED=0
archives:
- id: kflex
  builds:
  - kflex
release:
  draft: false
  prerelease: auto
  mode: keep-existing